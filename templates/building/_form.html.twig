{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-primary">{{ button_label|default('Créer l\'immeuble') }}</button>
        <a href="{{ path('building_index') }}" class="btn btn-primary">Retour à la Liste des immeubles</a>
    </div>
{{ form_end(form) }}
